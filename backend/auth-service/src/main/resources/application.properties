spring.application.name=auth-service
server.port=8081

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/auth-service
spring.datasource.username=postgres
spring.datasource.password=1234
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Secret
app.jwt.secret=i0eCF0g/9rj5xLQOpPT2xWHDyKtqzjnE220yTdrjJwI8/w1NtH7xB9/T8MqqHaAn
app.jwt.expiration=86400000

# Onboarding + OTP configuration
app.registration.urlBase=http://localhost:4200/register
app.verification.ttl-minutes=1440
app.otp.ttl-seconds=300

# Mail sending: configure to enable SMTP (and activate 'mail' profile if desired)
# spring.mail.host=smtp.example.com
# spring.mail.port=587
# spring.mail.username=apikey_or_user
# spring.mail.password=secret
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# app.mail.from=noreply@konecta-erp.com

# To prefer console logs over SMTP, do nothing.
# To force console logs explicitly, leave mail host unset or set spring.profiles.active=dev
# To enable SMTP service selection via profile:
# spring.profiles.active=mail
